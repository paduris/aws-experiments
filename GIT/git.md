**`git init`** 

**`git log`**

**`git status`**

**`git add <fileName>`**

**`git commit -m " Comments "`**

**`git rm <fileName>`**

**`.gitignore`**

**`git clone <to be cloned repo> <local-repo>`**

**`git push origin master`**

git branch

fast forward merge


git log --all --decorate --oneline --graph

